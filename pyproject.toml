[project]
name = "upside-my-app-01"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
]

[tool.uv]
dev-dependencies = [
    "pytest==9.0.3",
    "pytest-asyncio>=0.26",
]